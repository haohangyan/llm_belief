[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-belief"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "indra>=1.22",
    "indra_db>=0.0.1",
    "openai>=1.0",
    "pystow>=0.5",
]

[tool.setuptools]
packages = ["llm_belief"]
